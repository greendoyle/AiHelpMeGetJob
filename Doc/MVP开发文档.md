# 安卓AI求职辅助工具（MVP）\- 开发文档

# 一、文档说明

本文档基于需求文档、技术选型、4大里程碑拆分，用于指导开发、验收、联调，所有内容聚焦MVP版本，明确开发标准、验收要求及技术实现要点，确保开发过程规范、交付成果符合需求。

# 二、最终确认技术栈

- 开发语言：**Kotlin（原生Android）**（优先选用，语法简洁、适配Android原生开发，提升开发效率）

- 自动化能力：**Android 无障碍服务**（核心实现模拟用户操作、页面信息抓取，无需额外依赖）

- 本地存储：**MMKV**（腾讯开源高性能键值对存储，替代SharedPreferences/SQLite，适配轻量配置存储场景）

- 网络请求：**OkHttp \+ Retrofit**（Android行业标准组合，稳定高效，适配OpenAI兼容API调用）

- UI组件：Android 原生View、RecyclerView、Dialog、悬浮窗（极简设计，聚焦功能，降低开发复杂度）

- 最低版本：**Android 10\.0（API 29）**（适配主流安卓机型，确保无障碍服务正常运行）

- 架构：简单MVP（页面与业务逻辑分离，轻量无过度设计，适配MVP快速开发需求）

# 三、开发里程碑与可验收清单

## 里程碑1：基础框架搭建（底座可运行）

1. **工程初始化**

    - 功能：创建Kotlin原生Android项目，配置MMKV、OkHttp、Retrofit核心依赖，配置项目基础参数（包名、最小SDK版本等）

    - 验收：项目可正常编译、打包，安装到安卓10\.0及以上机型可正常启动，无编译报错、无启动崩溃

2. **权限引导与检测**

    - 功能：实现无障碍服务权限、悬浮窗权限的引导跳转（跳转至系统设置页），封装权限状态检测工具类，判断权限是否开启

    - 验收：APP启动后可检测权限状态，未开启时提示引导，点击引导可跳转至对应系统设置页，权限开启后可正常进入主页面

3. **MMKV存储封装**

    - 功能：封装MMKV统一工具类，提供存、取、删、清空等基础方法，适配简历信息、API配置、筛选条件等数据的存储需求

    - 验收：可正常存储/读取简历信息、API URL、API Key、筛选条件等数据，APP重启后数据不丢失，删除操作可正常生效

4. **网络请求封装**

    - 功能：基于OkHttp\+Retrofit封装网络请求工具类，支持POST请求、JSON数据解析，处理超时、请求失败等异常场景

    - 验收：可成功调用OpenAI兼容API，能正常发送请求、接收返回结果，异常场景（如网络中断、API无效）有明确回调

5. **5页面基础UI**

    - 页面：首页（任务运行）、参数配置页、AI模型设置页、简历录入页、历史记录页

    - 验收：5个页面布局完整，无布局错乱，页面间跳转正常（支持返回、快捷跳转），无点击崩溃、滑动崩溃

## 里程碑2：无障碍服务与自动化（操作可执行）

1. **无障碍服务注册**

    - 功能：在AndroidManifest中配置无障碍服务，实现AccessibilityService子类，处理服务启动、停止逻辑，设置服务优先级

    - 验收：无障碍服务可正常启动，启动后在后台稳定运行，不被系统轻易杀死，服务状态可被APP正常检测

2. **页面节点识别**

    - 功能：通过无障碍服务捕获页面控件节点，根据BOSS直聘的包名、控件文本、控件ID，判定当前页面是否为岗位列表页、岗位详情页、聊天输入框

    - 验收：页面判定准确率100%，无误判、漏判，切换BOSS直聘页面后可快速识别当前页面类型

3. **岗位信息抓取**

    - 内容：通过控件节点抓取岗位名称、公司名称、薪资范围、工作城市、岗位JD、是否外包等核心信息

    - 验收：抓取的信息完整、无乱码、无空值，与BOSS直聘页面显示内容一致，抓取响应时间≤1秒

4. **模拟操作封装**

    - 功能：封装模拟点击、滑动（上下滑动）、文本输入、返回键操作等方法，适配BOSS直聘页面操作逻辑

    - 验收：模拟操作稳定、无跳过步骤、无闪退，点击位置准确，滑动幅度符合真人操作习惯，输入文本无错乱

5. **防风控逻辑**

    - 功能：实现随机停留（1\-3秒）逻辑，支持用户自定义打招呼发送间隔，设置每日最大打招呼次数限制

    - 验收：操作节奏接近真人，无机械连续点击，发送间隔、每日次数符合用户配置，无违规操作痕迹

6. **任务控制**

    - 功能：实现任务启动、暂停、停止功能，封装任务状态回调，同步更新APP界面状态

    - 验收：点击启动/暂停/停止按钮即时生效，任务状态（运行中、暂停、已完成）切换正确，无状态错乱

## 里程碑3：AI匹配与自动话术（核心AI链路）

1. **简历信息管理**

    - 功能：实现简历信息录入、编辑、保存、本地读取功能，表单支持姓名、求职意向、工作年限、学历、核心技能等关键信息

    - 验收：信息录入后可正常保存，支持随时编辑修改，读取的信息与录入内容一致，仅存储在本地，不上传任何服务器

2. **筛选条件过滤**

    - 内容：根据用户配置的岗位关键词、工作城市、期望薪资、外包选项，对抓取的岗位进行过滤

    - 验收：不符合筛选条件的岗位直接跳过，不进入AI匹配环节，筛选逻辑准确，无漏筛、误筛

3. **OpenAI兼容API调用**

    - 功能：根据用户填写的API URL、API Key，拼接Prompt（结合岗位JD、用户简历），发起API请求，解析返回结果

    - 验收：支持用户自定义API配置，点击“测试API”可验证连通性，能正常获取AI返回的文本内容，解析无错乱

4. **JD匹配打分**

    - 功能：通过AI接口对岗位JD与用户简历进行匹配打分，获取打分结果后，与用户设置的阈值进行对比，过滤低匹配岗位

    - 验收：低于阈值的岗位不进入话术生成环节，高于阈值的岗位正常推进，打分结果准确，阈值判断逻辑无误

5. **打招呼话术生成**

    - 功能：基于岗位JD和用户简历，通过AI接口生成个性化打招呼话术，去除返回结果中的冗余内容（如无关提示、格式符号）

    - 验收：生成的话术干净、贴合岗位需求，无冗余内容，可直接发送给HR，话术生成响应时间≤3秒

6. **全自动发送链路**

    - 流程：岗位抓取→筛选条件过滤→AI匹配打分→话术生成→模拟发送打招呼话术

    - 验收：整个链路无需人工干预，自动执行，发送成功后记录相关数据，无流程阻塞、无步骤遗漏

## 里程碑4：历史记录\+整合\+稳定验收（可交付）

1. **历史记录存储**

    - 内容：存储岗位信息（名称、公司、薪资等）、匹配分数、打招呼话术、发送时间、发送状态（成功/失败）

    - 验收：每执行一次打招呼操作，相关数据自动落库，无数据丢失、无重复记录，重启APP后可正常读取

2. **历史列表与筛选**

    - 功能：以列表形式展示历史记录，支持按时间（今日、昨日、近7天）、匹配分数范围筛选，支持单条删除、批量删除

    - 验收：列表滚动流畅，无卡顿，筛选功能准确，删除操作即时生效，删除后数据不再展示

3. **实时状态面板**

    - 内容：在首页、悬浮窗展示任务运行状态（运行中、暂停、已完成）、已浏览岗位数量、已打招呼数量、当前岗位匹配分数

    - 验收：数据实时刷新，无延迟，与实际操作数据一致，悬浮窗状态同步更新

4. **异常与容错**

    - 场景：API调用失败、无障碍服务断开、检测到可能被平台风控、网络中断

    - 验收：出现异常时自动暂停任务，弹出明确的提示信息（如“API调用失败，请检查配置”），APP不崩溃、不闪退，异常解决后可正常重启任务

5. **性能与稳定性**

    - 标准：APP连续运行2小时无崩溃、无ANR（应用无响应），运行时占用手机内存≤100MB，不影响BOSS直聘及其他APP正常使用

    - 验收：通过稳定性测试，满足性能标准，无内存泄漏、无卡顿，操作流畅

6. **全流程验收**

    - 流程：参数配置→启动任务→自动抓取岗位→筛选→AI匹配→话术生成→发送→查看历史记录

    - 验收：MVP所有必做功能完整可用，流程无阻塞、无异常，符合需求文档中的边界界定，可交付使用

# 四、核心功能实现说明（极简版）

1. **自动化实现**

    - 核心依赖：Android 系统无障碍服务（AccessibilityService），通过监听页面控件变化，捕获目标页面节点

    - 模拟操作：调用performAction方法，实现模拟点击、文本输入、上下滑动、返回等操作，贴合真人操作逻辑

    - 页面判断：通过包名（BOSS直聘包名）\+ 控件文本/ID组合判定，确保页面识别准确性

2. **AI调用实现**

    - 请求地址：用户在AI设置页面填写的OpenAI兼容API URL

    - 请求头：携带Authorization认证（格式：Bearer \{API Key\}），确保API调用权限

    - 请求体：采用标准OpenAI请求格式，包含model（模型名称）、messages（Prompt内容）、temperature（生成随机性）等参数

3. **本地存储说明**

    - 存储介质：仅使用MMKV进行本地存储，无任何服务器部署，不涉及数据上传

    - 存储数据：用户简历信息、筛选条件配置、AI API配置（URL/Key）、历史操作记录，所有数据仅保存在用户手机本地

4. **防风控规则**

    - 核心逻辑：模拟真人操作节奏，设置随机停留时间、自定义发送间隔、每日发送上限，避免机械性操作

    - 合规性：不破解BOSS直聘平台规则，不篡改页面数据，不模拟网络协议，仅通过无障碍服务模拟真人手动操作，降低被平台检测、封禁的风险

# 五、权限清单（仅2个核心权限）

1. 无障碍服务权限（android\.permission\.BIND\_ACCESSIBILITY\_SERVICE）：核心权限，用于模拟用户操作BOSS直聘、抓取岗位信息

2. 悬浮窗权限（android\.permission\.SYSTEM\_ALERT\_WINDOW）：用于显示悬浮球、任务运行状态，提供快捷启动/暂停控制

说明：无其他额外权限，不获取用户手机通讯录、相册、位置等隐私数据，符合Android隐私规范。

# 六、禁止实现范围（需求边界）

- 不实现自动投递简历功能，简历投递需用户手动完成

- 不获取、不存储用户BOSS直聘账号密码，不绑定BOSS直聘账号

- 不内置本地AI模型，仅支持调用用户自定义的OpenAI兼容API，不提供API Key

- 不适配模拟器、云手机、平板设备，仅适配安卓10\.0及以上手机

- 不做商业化功能（如会员订阅、广告变现、付费解锁等）

- 不支持多招聘平台适配，仅适配BOSS直聘

- 不支持批量导入/导出简历，仅支持用户手动录入简历信息

# 七、验收标准（核心交付要求）

1. 功能完整：覆盖MVP所有必做功能，无遗漏核心需求，符合需求文档及本开发文档要求

2. 流程通顺：从参数配置→任务启动→自动运行→历史记录查询，全流程无阻塞、无异常

3. 稳定可靠：连续运行2小时无崩溃、无ANR、无卡顿，内存占用≤100MB，无障碍服务运行稳定

4. 合规安全：无隐私数据获取、无数据上传行为，不破解平台规则，模拟操作符合真人习惯，无风控风险

5. 易用性：UI界面简洁，操作路径清晰，权限引导明确，无复杂操作步骤，符合用户使用习惯

> （注：文档部分内容可能由 AI 生成）
